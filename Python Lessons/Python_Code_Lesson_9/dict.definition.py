# Example 1: dict.definition.py
# In this example we see the definition of two dictionaries
empty = {}
person = {
    "grade": 13,
    (1,2): 29,
    "name": "Jim",
}

print(person)
print(type(person))

#Comments:
#- Each key must exist exactly once.
#- The last value key may be followed by a comma.